[% USE XML.Escape; %]


[% # Dojo dropdown menu button %]
    
    <script type="text/javascript">include_contrib = 1;</script>

[% # No JavaScript block %]
    <noscript>
          <p>
          <form method="get" action="[% c.uri_for(root, 'search') %]">
          [% search_query('lang') %]
          <select name="lang">
                  <option value="">[% c.loc("Limit by contributor:") %]</option>
            [% FOREACH facet IN response.facet.contributor %]
                  <option value="[% facet.name | xml_escape %]">[% label.contributor.${facet.name} | xml_escape %] ([% facet.count %])</option>
            [% END %]
          </select>
            <button type="submit">[% c.loc("Search") %]</button>
          </form>
          </p>
    </noscript>
