<?xml version="1.0" encoding="utf-8"?>

<profile name="page" version="1.0" standalone="yes">
  <desc>
    A page from a book, issue, or other document which consists of
    multiple pages. This can be a literal page, or it can be a single
    image comprising multiple (e.g. facing) pages.
  </desc>

  <include file="global.xml"/>

  <field name="pkey" required="true">
    All pages are required to belong to a valid container.
  </field>

  <field name="seq" required="true">
    This field is required for page data.
  </field>

  <include file="description.xml"/>

</profile>

