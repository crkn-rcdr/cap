Standard XML Response


<response>
  <req>$request_uri</req>
  <ok>1 if successful; 0 if not</ok>

  <!-- If OK == 0, the following is also included -->
  <status>HTTP status code if OK == 0; otherwise not present</status>
  <error>Additional error code info</error>

  <!-- Otherwise, request-specific data follows -->
</response>


JSON

{
    "ok" : boolean, // true if the request was successful, otherwise false
    "req" : string, // the request uri

    // If ! ok
    "status" : int,
    "error" : string,

    // Otherwise, data follows
}


CORE USER API

Synopsis

    /$portal/$iface/
    /$portal/$iface/page/$name
    /$portal/$iface/search/pages
    /$portal/$iface/search/titles
    /$portal/$iface/show/$key
    /$portal/$iface/login
    /$portal/$iface/logout


ADMINISTRATIVE API

